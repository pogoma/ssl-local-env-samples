# Przykład dla angulara+nx

## Konfiguracja
Do elementu serve workspace.json dodaj obiekt:
```json
 "options": {
  "ssl": true,
  "sslKey": "klucz_prywatny.pem",
  "sslCert": "certyfikat.pem"
}
```

Ścieżki względne do workspace.json
